<div id="<%= dom_id(book, :like_button) %>">
  <% if logged_in? && current_user.like?(book) %>
    <%= render 'likes/like', book: book %>
  <% else %>
    <%= render 'likes/unlike', book: book %>
  <% end %>
</div>

